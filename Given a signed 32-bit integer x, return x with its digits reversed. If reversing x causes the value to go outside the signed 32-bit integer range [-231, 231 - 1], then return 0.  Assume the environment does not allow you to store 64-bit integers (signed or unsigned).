class Solution(object):
    def reverse(self, x):
        """
        :type x: int
        :rtype: int
        """
        rev = 0
        while x != 0:
            digit = int(x % 10)
            x = int(x / 10)   
            rev = rev * 10 + digit
        return rev
